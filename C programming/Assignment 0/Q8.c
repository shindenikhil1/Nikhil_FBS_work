#include <stdio.h>

int main() {
    int length = 10;
    int width = 5;
    int perimeter;

    perimeter = 2 * (length + width);

    printf("Perimeter of Rectangle = %d", perimeter);

    return 0;
}